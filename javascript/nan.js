console.log(typeof NaN);

// it doesn't actucally mean not a number, but rather it guess
// notion of an invalid number;

// we can use NaN, when we want to return invalid number.
// example - returning index as -1 in javascript.


console.log(NaN == 100);
console.log(100 == NaN);
console.log(NaN == NaN);
// it's only primitive which doesn't equal to itself



console.log(typeof NaN == typeof NaN);

